model_name: "student_stage2_static_flap_v1"

description: "First-round static calibration using flapwise tip-load and uniform-load benchmarks."

alpha_flap: 1.01
alpha_edge: 1.0
alpha_torsion: 1.0

calibration_cases:
  - "cantilever_tipload"
  - "cantilever_uniformload"

notes: "alpha_flap selected by minimizing the stage-2 static objective over tip-load + uniform-load."